[build]
  base = "/"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "netlify-plugin-cache"
  [plugins.inputs]
    paths = [
      "node_modules/.cache",
    ]